# NexAsk Sources

Place NexAsk-only host files here as the standalone app boundary is extracted from the shared runtime.

Examples of future moves:

- NexAsk-specific bootstrap and app lifecycle composition
- NexAsk-only settings surface
- NexAsk-only release or packaging glue
